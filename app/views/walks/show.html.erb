<h1>Walks#show</h1>

<div>
  <%= @walk.name %>

  <% @walk.waypoints.each do |waypoint| %>
    <%= waypoint.description %>
    <%= waypoint.address %>
    <%= waypoint.latitude %>
    <%= waypoint.longitude %>
  <% end %>

</div>
